import prisma from "../prisma/client";
import { PrismaClientKnownRequestError } from "@prisma/client/runtime/library";
import { Prisma, User } from "@prisma/client";

class ProjectService {

// POST - /api/projects - Create a new project (only verified NGOs/admins).

// GET - /api/projects -List all projects with filters (status, city, creator, etc.).

// GET - /api/projects/:id - Get details of a specific project.

// PUT - /api/projects/:id - Update project details (admin/creator only).

// PATCH - /api/projects/:id/status - Change project status (PENDING, APPROVED, COMPLETED, etc.).

// DELETE - /api/projects/:id - Delete a project (admin only).

}